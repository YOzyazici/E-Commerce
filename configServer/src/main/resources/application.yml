spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/YOzyazici/ECommerce-config-server.git
  profiles:
    active: dev
server:
  port: 8050